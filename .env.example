# Environment variables for local/dev and reference for Railway
# Railway injects PORT automatically; default is fine locally
PORT=5000
NODE_ENV=production

# Database (required for production)
# Get this from Railway dashboard under "PostgreSQL" service
DATABASE_URL=postgresql://username:password@host:port/database

# Optional: Neon database (alternative)
# NEON_DATABASE_URL=postgresql://username:password@host:port/database